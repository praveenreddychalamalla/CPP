#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
/* 
   stable_sort() is similar to sort() except that it is stable in nature. O(nlogn)
   stable_sort requires more memory compared to that of sort(). If no extra memory is available  stable_sort() takes O(n loglogn)

   template<class RandomAccessIterator>
   void stable_sort (RandomAccessIterator first,RandomAccessIterator last) ; Elements are compared using < operator

   template<class RandomAccessIterator, class Compare>
   void stable_sort (RandomAccessIterator first, RandomAccessIterator last, Compare comp ) ; Elements are compared using comp
    
   Note: Equivalent elements are not guaranteed to keep their original relative order.
*/
int main(){

    return 0;
}